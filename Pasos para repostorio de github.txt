Pasoos para iniciar github y usarlo como almacenamiento jajsajas
Inicializamos Github en espacio donde este la carpeta a subir

git init
git status
git add .
git commit -m "Mensaje"
git config --global user.email edalramt@gmail.com
git config --global user.name Tiburonsin
git branch -M main
git "Copiamos la direccion del repositorio remoto"
git push -u origin main
